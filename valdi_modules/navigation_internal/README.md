# Navigation Internal

This module contains TypeScript-only implementation of the INavigator interface from the navigator module. Used only **for internal-only features** to simplify integration where it's not important to use platform-specific navigation functionality (Deck, UIKit presentations etc.).
